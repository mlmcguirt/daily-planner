# Loaded by `vite build --mode demo`. Tracked on purpose: it must exist inside the
# throwaway worktree that scripts/deploy-demo.mjs builds in.
#
# NOT `VITE_DEMO=1 npm run build` — that is bash syntax, and npm shells scripts
# through cmd.exe on Windows, where it fails outright.
VITE_DEMO=1
